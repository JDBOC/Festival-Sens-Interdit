<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>SessionDate</th>
            <th>Location</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
    {% for session in sessions %}
        <tr>
            <td>{{ session.id }}</td>
            <td>{{ session.sessionDate ? session.sessionDate|date('d-m-Y à H:i') : '' }}</td>
            <td>{{ session.location }}</td>
            <td>
                <a href="{{ path('session_delete', {'id': session.id}) }}">delete</a>
                <a href="{{ path('session_edit', {id: session.id, content_id : content.id}) }}">edit</a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="4">no records found</td>
        </tr>
    {% endfor %}
    </tbody>
</table>


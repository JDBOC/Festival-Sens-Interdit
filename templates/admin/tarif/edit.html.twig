{% extends 'adminBase.html.twig' %}

{% block title %}Edit Tarif{% endblock %}

{% block body %}
    <h1>Modification d'un tarif</h1>

    {{ include('admin/tarif/_form.html.twig', {'button_label': 'Enregistrer'}) }}


    <div class="row">
        <div class="col-2">
            <a href="{{ path('tarif_index') }}" class="btn btn-info btn-sm">Retour</a>
        </div>
        <div class="col-2">
            {{ include('admin/tarif/_delete_form.html.twig') }}
        </div>
    </div>


{% endblock %}

{% extends 'adminBase.html.twig' %}

{% block title %}Edition{% endblock %}

{% block body %}
    <h1>Editions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ edition.name }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ edition.status }}</td>
            </tr>
            <tr>
              <th>Date de début</th>
              <td>{{ edition.dateDebut|date("d/m/Y") }}</td>
            </tr>
            <tr>
              <th>Date de fin</th>
              <td>{{ edition.dateFin|date("d/m/Y") }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('edition_index') }}" class="btn btn-info btn-sm">Retour</a>

    <a href="{{ path('edition_edit', {'id': edition.id}) }}" class="btn btn-info btn-sm">Modifier</a>

    {{ include('admin/edition/_delete_form.html.twig') }}
{% endblock %}

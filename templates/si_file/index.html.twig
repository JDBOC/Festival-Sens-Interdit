{% extends 'adminBase.html.twig' %}


{% block body %}
    <h1>SiFile index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Type</th>
                <th>Fichier</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for si_file in si_files %}
            <tr>
                <td>{{ si_file.id }}</td>
                <td>{{ si_file.name }}</td>
                <td>{{ si_file.fileTypeName }}</td>
                <td>
                    <img alt="img_fichier" src="/upload/file/{{ si_file.mediaFileName }}" style="width: 200px; height: auto" />
                </td>
                <td>
                    <a href="{{ path('file_show', {'id': si_file.id}) }}">show</a>
                    <a href="{{ path('file_edit', {'id': si_file.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('file_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edition{% endblock %}

{% block body %}

    <h1>Edition</h1>
<div class="col-md-3">
    {{ include('content/_form.html.twig', {'button_label': 'Save'}) }}
    <button type="button" class="btn btn-outline-primary"><a href="{{ path('content_index') }}">back to list</a></button>
    {{ include('content/_delete_form.html.twig', {'button_label': 'Delete'}) }}
</div>
  {% endblock %}

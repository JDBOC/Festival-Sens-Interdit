{% extends 'base.html.twig' %}

{% block stylesheets %}
        {{ encore_entry_link_tags('newsIndex') }}
{% endblock %}
{% block body %}

	    <div class="container content">
	    	<div class="row">
	    {% for content in contents %}
	    	<div class=" col-12 mb-3">
		    	<div class="card-news">
					<div class="row">
						{% if loop.index %2 == 0 %}
						<div class="col-3">
							<img src={% if content.thumbnail.mediaFileName is defined %}
										"upload/file/{{content.thumbnail.mediaFileName}}"
									{% else %}
										"upload/file/blackThumbnail.jpg"
									{% endif %}
							 class="card-img" alt="...">
						</div>
						<div class="card-body pt-0 col-9">
							<h3 class="mb-0">{{content.titleFr}}</h3>
							<p>{{content.contentFr|raw}}</p>
							<a href="{{ path('show_content', {'slug': content.slug}) }}" class="float-right"><button class="btn btn-outline-primary">en savoir plus</button></a>
						</div>
						{% else %}
						<div class="card-body pt-0 col-9">
							<h3 class="mb-0">{{content.titleFr}}</h3>
							<p>{{content.contentFr|raw|length > 100 ? content.contentFr|striptags|slice(0,400)|raw : content.contentFr|raw}}</p>
							<a href="{{ path('show_content', {'slug': content.slug}) }}" class="float-left"><button class="btn btn-outline-primary">en savoir plus</button></a>
						</div>
						<div class="col-3">
							<img src={% if content.thumbnail.mediaFileName is defined %}
										"upload/file/{{content.thumbnail.mediaFileName}}"
									{% else %}
										"upload/file/blackThumbnail.jpg"
									{% endif %}
							 class="card-img" alt="...">
						</div>
						{% endif %}
					</div>
				</div>
			</div>
		{% endfor %}
			</div>
		</div>

{% endblock %}

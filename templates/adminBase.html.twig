<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        {% endblock %}
            {{ encore_entry_link_tags('admin') }}
        <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
    </head>
    <body>
    <div class="container-fluid px-0">
        <nav class="navbar navbar-expand-lg ">
            <a href="{{ path('admin_index') }}" class="navbar-brand mb-0 col-8 col-md-2 col-lg-1" style="color: #FFFFFF;">Sens Interdits</a>
            <button class="navbar-toggler text-light" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('show_index') }}">Contenu</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('tarif_index') }}">Tarifs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('edition_index') }}">Editions</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('partner_index') }}">Partenaires</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('theme_index') }}">Themes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('sifile_index') }}">Fichiers</a>
                    </li>
                </ul>
            </div>
            {# <a class="nav-link text-light col-6 col-lg-2" href="{{ path('app_editPassword') }}">Modifier MDP</a>
            <a class="nav-link text-light col-6 col-lg-2" href="{{ path('app_logout') }}">Se déconnecter</a> #}
            <div class="row col-lg-4">
                <a class="nav-link mx-auto text-light" href="{{ path('app_editPassword') }}">Modifier MDP</a>
            <a class="nav-link text-light mx-auto" href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
        </nav>
        <div class="container px-0">
            {% block body %}{% endblock %}
        </div>
    </div>
        {% block javascripts %}
                 {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>

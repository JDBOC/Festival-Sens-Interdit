<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('admin') }}
        {% endblock %}
        <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
    </head>
    <body>
    <div class="container-fluid px-0">
        <nav class="navbar navbar-expand-lg ">
            <a href="{{ path('admin_index') }}" class="navbar-brand mb-0" style="color: #FFFFFF;">Sens Interdits</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('show_index') }}">Spectacles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('tarif_index') }}">Tarifs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('edition_index') }}">Editions</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="#{#{{ path('partner_index') }}#}">Partenaires</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">News</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('sifile_index') }}">Fichiers</a>
                    </li>
                </ul>
            </div>
            <a class="nav-link text-light" style="text-align: right;" href="{{ path('app_editPassword') }}">Modifier MDP</a>
            <a class="nav-link text-light" style="text-align: right;" href="{{ path('app_logout') }}">Se déconnecter</a>
        </nav>
        <div class="container">
            {% block body %}{% endblock %}
        </div>
    </div>
        {% block javascripts %}
                 {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
